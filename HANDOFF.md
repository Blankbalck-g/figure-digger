# figure-digger 交接文档

## 2026-09-20 当前状态（优先于下方历史记录）

主流程已收敛为专业化轻量 Agent：分类/轴读取后，由一次 planner 返回系列清单、
line/marker 身份、角色、图例禁区、预期范围和粗锚点；代码做像素追踪与坐标换算；最后一次
critic 只给 merge/drop/add/extend 指令。局部 gap Agent 仅在真实范围缺失时触发，每面板最多 3 次。

- 图例现在是**整框硬禁区**，本地矩形检测与 planner `exclude_regions` 双路提供；框内样本线、
  marker、文字均不能成为数据证据。成功锚点不再画进最终 verify。
- 正常 VLM 路径不再单独调用 `name_series` 后又让 planner 重读，减少一轮重复理解。
- 2024 Figure 4（三条 Vapor penetration）：3 条均提取，图例无污染，spot check 6/6。
- 2023 Figure 5：`S_tip`、`S_tail`、`Model` 均完整；`S_tail x=0.057~4.994`，全部 x 回跳为 0，
  spot check 6/6。
- 2026 Figure 10 左图红线起点误接坐标边框已修复：根因是 gap Agent 对并不存在的起点缺口
  补了框外/贴框锚点。现在模型补点必须严格在绘图区内，且只允许进入代码检测到的真实长断口；
  `Heatedtip` 起点由负 x/竖直伪段变为 `x≈0.009, y≈0.805`，spot check 6/6。
- CLI 默认仅打印面板结论、最终路径和一行 VLM 用量；`--verbose` 才显示逐图细节。
  `gaps/review/debug/select` 默认走临时目录并自动清理，`--keep-intermediates` 才落盘。
- 模板参数已收敛为三段链：**论文级工况知识库 → 图级工况/事实绑定 → 代码单位换算与模板填充**。
  模型每篇从分栏全文证据建一次 `facts/conditions`，后续面板只绑定 ID；PDF 文本漏掉但图内明写的值可以受控 `panel_facts` 写回知识库。
  代码负责标量检查、单位换算、直径→半径、`same_as`与冲突拒绝，原值/原单位/证据/事实 ID 都可审计。
- 参数真实接口回归：2026 Figure 10 提取出 273 K、35 MPa、0.1 MPa 和 363/273 K；
  2024 正确绑定 24.1 kg/m³、700 bar、100 μm 孔径及 800/946/1150 K；2007 从图内 `700 BAR / 21 g/L / 5×0.18×150 TIP` 入库并换算为 `70 MPa / 0.021 g/cm³ / 0.009 cm`，后两图复用；
  2023 多候选反例只填唯一明确的 298 K，多值列表/范围不再默认取首值。定向测试约 `$0.001–$0.009/篇`。
- 产品方向已确定为桌面端，不再规划 DSH 插件；当前先保持 CLI 核心稳定，后续桌面 UI 直接调用同一流水线。
- 2007 Figure 8 属低质量、黑线+marker 混合编码的罕见压力样本，不再继续为它堆专用算法；
  planner 已能排除 PULSE，但该图不作为当前泛化目标。

下方第 3 节保留的是早期问题账本，其中“未解决”结论可能已被以上状态覆盖。

> 写给下一个接手这个项目的 AI/人。**第 3 节是"用户提的问题哪些没解决好"**，请先读那一节。

## 0. 一句话现状

`fig-extract/` 是一个"论文 PDF → 曲线数据"的流水线（`run.py` 唯一入口，模块在 `dig/`）。
设计原则是 **VLM 负责理解（有哪些曲线、什么颜色、哪段被压住、轴是什么、参数是多少），
代码负责像素精度（追踪、补全、换算）**。

目前：能跑通、能批量出 CSV/质检图/报告/模板文件，黑白图也能出数据；已对重合断裂、
图例污染、起点接边框和模板参数缺失做了真实图回归。剩余主要风险是未覆盖版式的轴标定、
近竖直尖峰和闭环碎片串接（见第 3 节历史问题账本）。

## 1. 怎么跑（用户环境）

```powershell
cd D:\WebPlotDigitizer-master\fig-extract
$py = "C:\Users\lenovo\anaconda3\envs\pytorch\python.exe"   # 必须用这个解释器（装了 cv2/pdfplumber/rapidocr）
& $py run.py doctor                                          # 自检：依赖 + key

# 单篇（--want 选图 + 提取 + 模板输出）
& $py run.py all papers\2024-01-3408.pdf --out out --vlm --want "喷雾贯穿距与时间的关系图象" --template my_data.dat
# 批量
& $py run.py batch papers --out out --vlm --jobs 4 --template my_data.dat
# 只重出模板文件 / 只重跑提取
& $py run.py extract out\<论文>\<论文>_config.json --template my_data.dat
& $py run.py extract out\<论文>\<论文>_config.json --no-template
```

- key 在 `deepseek_key.txt`（未入库）；`.vlm_cache/` 是结果缓存，命中不花钱；`.templates/` 是模板编译缓存。
- **`--template` 必须配 `--vlm`**：模板要靠模型编译成代码；没有 VLM 会打印"模板不可用，只出 CSV"。
- 默认输出：`out/<论文>/{csv,verify,report.md,extract_results.json,templates,panels,figures}`。
  - `templates/<模板名>/<系列名>.<ext>` ← 按用户模板渲染的结果
  - `--keep-intermediates` 才额外保留 `gaps/review/debug/select`
- `papers/` 里有 4 篇真论文（用户手头的）：2007-01-0673、2023-01-1635、2024-01-3408、2026-01-0340；`out/` 已在 2026-09-20 用真实 DeepSeek 接口做过定向复跑。

## 2. 代码结构 / 数据流

```
run.py                   唯一入口：analyze / extract / confirm / all / batch / doctor
dig/
  triage_pdf.py          PDF 分诊：找图与图注、判断矢量/位图
  split_panels.py        多子图切分（轮廓框 + L 形坐标轴框）
  figure_index.py        图注↔图配对、--want 选图（本地编号匹配 → 缩略图对照图 → 正文通道）
  axis_ranges.py/ocr_ticks.py   刻度 OCR 与标定（VLM 模式下默认关）
  legend_colors.py       图例色块检测（配合整框禁区定位）
  extract_lines.py       颜色掩膜、逐列追踪 _walk、深色掩膜 dark_mask
  series_seed.py         **核心**：颜色解析、候选/实例、遮挡补全 complete_trace、
                         回线判据、标记中心、平台压缩、模型锚点插入
  batch_extract.py      单篇多面板批处理 + inner_boxes（图例/内嵌框排除）
  vector_extract.py      矢量直读（PDF 路径 + 刻度文字）
  verify_overlay.py      质检叠加图（左：点画回原图；右：数据重画）
  vlm_client.py          VLM 客户端（缓存/日志/用量/mock 应答）
  vlm_tasks.py           **全部提示词**：分类/轴读数/命名/选图/找曲线/复盘/缺口锚点
  condition_kb.py        论文级工况库、图级事实绑定、标量冲突检查与确定性单位换算
  template_out.py        模板编译与缓存、参数探针、全文证据包、三段参数 Agent 入口
  imgio.py               中文路径安全读写
tests/                   7 个回归脚本 + mock 服务器 + 矢量测试 PDF
```

数据流（位图路线）：

```
分诊 → 图注配对 → [--want 选图] → VLM 分类(非数据图跳过) → 切分面板 → 图例取色
  → VLM 读轴(轴标题/单位/数量级/是否符合需求) → VLM 找曲线(颜色/dark/closed/draw/overlaps/锚点)
  → 代码追踪(颜色优先+锚点消歧) → 遮挡补全(像素证据) → **模型读图补缺口锚点**
  → 去重合并 → [复盘轮：模型核对数量/归属] → 写 CSV + 质检图
  → 论文工况库（每篇一次）→ 图级 fact/condition 绑定 → 代码换算/冲突检查 → 模板渲染 → 报告
```

## 3. ⚠️ 用户提的问题：逐条对账（**未解决/部分解决都在这里**）

证据列来自用户最后一次真实运行（`out/`，2026-09-20 00:09，含 386320d 及之前的代码）。

| # | 用户提的问题 | 状态 | 证据 / 还差什么 |
|---|---|---|---|
| 3.1 | **曲线部分重合 → 只显示一种颜色 → 追出来的曲线不完整** | ❌ **未解决好**（本项目的核心痛点） | 见下方 3.1 展开 |
| 3.2 | **坐标轴映射不准**（数值不对） | ❌ **基本未解决** | 只做了 ×10⁻⁴ 数量级乘数 + 复盘轮的 `axis_fix`；**坐标框/刻度线的像素级联合标定没做**。见 3.2 |
| 3.3 | 一条曲线被识别成两条/多条（数量不对） | ⚠️ 部分解决 | 完全重复的轨迹按"同一实例/≥97% 重合"合并（2007 那次合并了 6 条）；**模型明说 overlaps 的不合并**，交给复盘轮。但"同色两条相近线"仍可能误合或漏合 |
| 3.4 | 画出来出现"回路"（闭合折返） | ✅ 已解决 | 回线只在模型说 `closed=true` 且"真围出一块面积"（面积/笔画 ≥2）时走轮廓法；用户 `out/` 里 loop 假轨迹 = 0 |
| 3.5 | 散点+折线被当成两条 / 散点处有"平台/阶梯" | ✅ 已解决（本地+用户图验证） | `draw` 语义（一条图例=一条曲线=一份数据）+ 标记中心当数据点 + 平台压缩。2023-01-1635 图 4：683→120 行、平台段 38→0 |
| 3.6 | 背景网格线被当成曲线 | ⚠️ 部分解决 | 黑线路线三道防线（横跨大半张且无起伏 / 该高度整幅都有线 / 40% 点在同一行）；2007 那次复盘轮还 drop 了 #6/#7 两条网格线。**很淡的点状网格仍可能漏** |
| 3.7 | 模板输出找不到 / 参数只有零星几个 | ✅ 已解决并做真实 API 回归 | 三段知识链已接入；4 篇论文覆盖双栏、老论文图内事实、单位换算、系列级绑定和歧义反例；见 `condition_knowledge.json` / `extract_results.json` / `report.md` |
| 3.8 | `--want` 只靠"模型觉得像" | ⚠️ 部分解决，且**曾误杀** | 轴读数顺带核对轴标题（x=时间/y=贯穿距），不符合就跳过。但 2026-01-0340 图 10 左图因**横轴标题被裁掉**被判 false → 整张图没出数据（已改判据：读不出按未知保留，见 4.1，未在真实 API 复跑） |
| 3.9 | 黑色文字/黑线被当曲线 | ✅ 已解决 | 黑线通道默认关闭；只有模型说 `dark=true` 才走深色掩膜 + 锚点路线 |
| 3.10 | 尖峰被削平 | ❌ **未解决** | 追踪取"每列竖切中心"，近竖直尖峰顶部偏低（实测 2026-01-0340 图 14 峰值 5.14 vs 真值 ~5.6）。要改成"沿行走方向的边缘点"，会动到所有曲线的取点约定，没做 |
| 3.11 | 闭环曲线被压住的半圈 | ❌ **未解决** | 掩膜碎成多块时只按可见弧出数据；需要"按端点把碎弧串成一条"再借另一条回线补 |
| 3.12 | VLM 没被用起来（纯像素方案不鲁棒） | ⚠️ 方向已改，但核心那一步还不够 | 模型现在决定：dark/closed/draw/overlaps/锚点/复盘/缺口锚点/轴标题/模板参数。但**"重合处到底往哪走"仍主要靠代码**（见 3.1） |
| 3.13 | 端到端真实 API 验证不足 | ✅ 参数链已真实验证；全量 batch 仍可继续扩测 | 已真实定向复跑 4 篇论文；2007 三图入库/复用和 2023 多候选拒绝均已现场验证，参数调用费用约 `$0.001–$0.009/篇`；曲线核心回归与 mock 链也通过 |

### 3.1 展开：重合处不完整（用户说了很多遍的那条）

**现象（用户实际数据）**：2024-01-3408 图 5，Diesel（蓝色圆点）前半段与 Tetradecane（绿色方块）
几乎重合，压在下面，颜色追踪只拿到 **x∈[0.398, 1.449]**，而 Tetradecane 是 **x∈[0.046, 2.001]**。
2023-01-1635 图 7 的 S_tail 也只从 x=2.38 才开始（T 型曲线前段被 Model 压住）。

**为什么一直没解决**（按时间顺序，避免下一个 AI 重走）：

1. 最初（`549cbb6`）只做"沿另一条曲线的像素轨迹"补全 —— 前提是隐藏段**精确贴着**另一条线。
2. 更深一层：`complete_trace` 的桥接要求「断口两端都是本颜色的真实像素 + 桥上有别的笔画覆盖」，
   而**曲线两端被压住**时没有"重新出现的像素"，只能靠 `_follow_occluder`（要求消失点 ≤6px 贴在另一条线上），
   实测经常不满足 → 不补。
3. `386320d` 才加上"让模型读图给锚点"（`run._repair_gaps` + `vlm_tasks.gap_anchors` +
   `series_seed.bridge_through_anchors`）：裁缺口那一小块（别的曲线画灰、已识别部分画本色）→
   模型给 2~3 个锚点 → 代码**只往缺口里插**。
4. **但 386320d 在真实运行里没生效**：2024 Diesel 的报告里既有"按标记中心出数据（19 个点）"又有
   "🧠 模型读图补的锚点（2 个）"，而 CSV 的 x 范围**没变** —— 因为散点分支随后用 marker 中心
   整条覆盖了 trace，把刚补的锚点抹掉了。`73f8548` 修了（保留标记范围之外的点），**但没在真实 API 复跑**。

**还没解决的深层原因 / 下一步**：

- 即便锚点插进去了，也只是 2~3 个点**直线连**（`bridge_through_anchors` 不插值），隐藏段形状是假的；
  要"沿两条可见段平滑延伸"或让模型给更多锚点（现在 prompt 只要 2~3 个，`max_tokens=400`）。
- 触发条件偏保守：只在这条线比"所有曲线的并集范围"短 >5%（两头）或断口 >4% 图宽时才问，
  且每面板最多 4 次调用。有些"看着够长但中段被压"的曲线不会触发。
- 锚点没有做**一致性校验**（例如"锚点必须在两条可见段端点的连线上/附近"），模型给错就跟着错。
- `markers_in_mask` 对密集标记检测不全（2024 Diesel 只认出 19 个），导致"点"文件本身也不完整；
  现在靠"线"文件弥补，但用户要的是完整曲线。

### 3.2 展开：坐标轴映射

- 现状：`AXIS_PROMPT` 让模型读 min/max/labels/scale/**multiplier（×10⁻⁴ 这类）/单位/轴标题**；
  `extract()` 用"配置的坐标框 + 这些数值"做线性映射；复盘轮可以返回 `axis_fix` 让该面板重跑。
- **没做**：用图像里的**刻度线/网格线像素位置**去反算比例（"代码测像素、模型读数值"的联合标定）。
  现在坐标框来自面板检测（`be._frame_of`，`73f8548` 之前还会误取图幅外框），框差几十像素，
  数值就整体偏 —— 这是"读数不对"最常见的根因。
- 另外：`AXIS_PROMPT` 的 min/max 完全靠模型目测，错了没有第二道独立校验（`--ocr` 默认关，
  spot check 只能发现"明显不一致"）。

## 4. 已完成的工作（按 commit，方便定位）

| commit | 做了什么 | 验证方式 |
|---|---|---|
| `73f8548` | ①模型补的锚点被标记中心覆盖 ②轴标题"读不出"被误判为不符而跳过 ③模板同名系列取线 | 本地 4 个测试通过；**未真实 API 复跑** |
| `386320d` | 模板输出不丢（参数由代码自报 `probe_params` + 单位提示 + 字符串转数字）、模型读图补缺口锚点 | mock + 用户图离线复现（Diesel 205→131/618→757）；用户真实运行已产出模板文件 |
| `58d3622` | 散点"阶梯"：标记中心当数据点、平台压缩；顺带修 `_frame_of` 误取图幅外框 | 用户图 2023-01-1635 图 4：683→120 行、平台段 38→0；新增两条测试 |
| `b262979` | `--template` 模板输出功能 + `--want` 的轴标题核对 | 新增 `test_template_out.py` / `test_axis_check.py`；mock 链路 |
| `79af4d1` | 模型决定 dark/closed/draw/overlaps/occluded；黑线路线；复盘轮；网格线三道防线；轴数量级 | 用户 `out/` 重跑对比：117→124 条、合并 32、回路 3→0、网格 8→1 |
| `549cbb6` | 重合段补全 `complete_trace`（像素证据）+ 质检图橙色圈标注补出来的点 | 2026-01-0340：图 10 两条虚线 430/495 → 845/847 点 |
| `efe8432` | **范式切换**：删掉"候选物件+模型判定"，改成"模型给曲线清单+锚点，代码从锚点追踪" | 矢量真值 4.44e-16；mock 链路 |
| 更早 | 目录重构（`dig/`）、LICENSE、打包、`.gitignore` 保护 key/论文/产物 | — |

## 5. 测试与回归（怎么自查）

```powershell
$py = "C:\Users\lenovo\anaconda3\envs\pytorch\python.exe"
& $py tests\test_trace_guards.py     # 回线/网格/平台/模型锚点插入 判据
& $py tests\test_review_round.py     # 复盘轮：编号语义 + 整链路（mock）
& $py tests\test_template_out.py     # 模板：编译一次即缓存/渲染/参数填/无参数也要出文件
& $py tests\test_axis_check.py       # 轴标题核对：符合/不符/读不出
& $py tests\test_vlm_pipeline.py     # VLM 链路（mock，需要 papers/ 里的 PDF）
# 矢量零误差回归（不需要 key）：run.py all tests/test_vector.pdf --out <tmp> --force
```

所有测试都用 `tests/test_vlm_mock.py` 的本地 HTTP mock（不需要 key、不联网）。
**注意**：mock 是"应答模板"，改提示词后 mock 的判据也要跟着改（`dig/vlm_client.mock_response`
的匹配顺序很关键：模板 → 参数 → 补缺口 → 复盘 → 找曲线 → 选图 …，顺序错会互相吞）。

## 6. 下一个 AI 建议的优先级

1. 做一次 4 篇论文的全量 batch 回归，重点统计参数字段覆盖率、空值是否有合理歧义理由、总 token/费用；当前定向真实回归已经满足“10 篇约 0.3 美元”预算，但全量面板仍应观测缓存收益。
2. 模板文件名目前由模板生成；不同面板若产生同名系列，后一个面板可能覆盖前一个文件。后续桌面端设计时应把“论文 / 图 / 面板 / 系列”作为显式层级，或在写盘层提供稳定的冲突命名策略。
3. **轴标定**（3.2）：实现“检测网格线/刻度线像素 → 用模型读到的刻度数值拟合 → 反算坐标框”，这是数值正确性的最大单点收益。
4. 尖峰取点（3.10）与闭环碎片串接（3.11）。
5. 桌面端：优先做任务选择、面板/质检图对照、参数证据审阅与手工覆盖，不要把当前轻量 Agent 拆成很多复杂步骤。

## 7. 坑与注意事项（踩过的）

- **不要用系统 python**：必须用 `C:\Users\lenovo\anaconda3\envs\pytorch\python.exe`。
- Windows 控制台是 GBK：跑脚本打印中文/emoji 前先
  `[Console]::OutputEncoding=[Text.Encoding]::UTF8; $env:PYTHONIOENCODING="utf-8"`，
  否则会 `UnicodeEncodeError`（emoji 🧠 尤其容易炸）。
- `out/`、`.vlm_cache/`、`.templates/`、`papers/`、`deepseek_key.txt` 都在 `.gitignore` 里，
  **不要提交**。`.templates/` 可以整个删掉（会重新编译，花一次调用）。
- 复现用户问题要用**用户已有的 config + 面板图**（`out/<论文>/...`），
  别自己重新 `analyze`（无 VLM 时轴范围是 None，面板会被跳过；而且 dpi/切分可能不同）。
- `--want` 用中文描述时不要带图号，否则会走本地编号匹配（`LABEL_NUM_RE`）。
- 改 `dig/series_seed.py` 的追踪参数（`MAX_JUMP/MAX_GAP`）会牵动很多行为，先跑
  `tests/test_trace_guards.py` 与矢量回归。

## 8. 用户最后一次真实运行的统计（作为基线）

```
2007-01-0673   4 面板  11 曲线  0 未追到  合并 6  复盘 1（drop 2 条网格线 + add 1）
2023-01-1635   1 面板   3 曲线  0 未追到  模型补锚点 1 处
2024-01-3408   2 面板   5 曲线  0 未追到  模型补锚点 1 处（但见 3.1：未真正生效）
2026-01-0340   1 面板   2 曲线  另一面板被轴标题核对跳过（误杀，见 3.8）
模板文件：out/<论文>/templates/my_data/*.dat 共 16 个
缺口裁剪图：out/<论文>/gaps/*.png 共 6 张
```

报告里能看到的关键行：`🧠 模型读图补的锚点`、`⚠️ N 段长断口未补`、`⇄ 合并`、
`模型复盘：merge/drop/add`、`轴标题 … 需求核对 ✅/❌/⚠️`、`⏭ 跳过原因`。
